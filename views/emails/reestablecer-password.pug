<!DOCTYPE html>
html(lang="en")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        style
            include ./email.css
    body
        h2 Reestablecer Password
        p Hola, has solicitado reestablcer tu password, haz click en el siguiente enlace para reestablacer, este enlace es temporal, si caduca, vuelve a solicitarlo. 
        a(href=`${resetUrl}`) Ir a enlace
        p Si no puedes acceder a este enlace, visita #{resetUrl}
        p Si no solicitaste este email, puedes ignorarlo.